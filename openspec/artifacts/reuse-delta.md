# Reuse Delta

## Promote After Proof

1. Next.js skill: SSR composition, application-owned ports, fixture/live adapters, and page-local state rule.
2. Standalone template: copy `.next/static` into local standalone output and use the same server for E2E/benchmark/Docker.
3. Browser benchmark guidance: wait for an explicit UI completion marker; publish V2 only from a clean tree.
4. Formatter scope: ignore vendored skills, contracts, control files, and generated token outputs.

## Keep Local

Fixture evidence, chart labels, comparison presentation, and product routes belong to #32. Apollo, Redux, BFF, and cloud scaffolds remain rejected.
